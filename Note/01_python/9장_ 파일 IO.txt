9장_ 파일 io

1절. txt파일 io
	with open(파일 상대경로|파일절대경로, ' r | w | a', encoding) as f: 
		list변수 = f.readlines() / 문자변수 = f.read()
		f.write(저장할 문자열) / f.writelines(문자list)


2절. 피클(pkl, picle, data) 파일 io
	with open(파일경로, 'rb | wb'(바이너리 모드로 연다.)
		객체변수 = pickle.load(f)
		pickle.dump(변수,f)


3절. csv파일 io
	with open(파일 경로, 'r | w | a', encoding) as f:
		reader =csv.reader(f); 2차원list = list(reader)
		writer = csv.writer(f); writer.writerows(2차원list)

		dic_reader = csv.DictReader(f); 딕셔너리list = list(dic_reader)
		dic_writer = csv.DictWriter(f); dic_writer.writerows(딕셔너리list)


4절. json 파일 io
	with open(파일경로,  'r | w | a', encoding) as f:
		딕셔너리 list - json.load(f) 
		json.dump(딕셔너리list, f, ensure_ascii=False, indent)

		객체list = json.load(f, object_hook=
					     딕셔너리를 객체로 return하는 함수명)
		json.dump(객체listm fm default=
					  클래스명. 객채를 딕셔너리 retrun함수명)


5절. h5/hdf5 파일 io
	import pandas ad pd
	데이터프레임변수 = pd.read_hdf(파일경로, key)
	데이터프레임변수.to_hdf(파일경로, key)